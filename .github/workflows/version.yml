# Auto-generated by gaji
# Do not edit manually - Edit workflows/version.ts instead
# Generated at: 2026-02-23T17:41:24Z

concurrency:
  cancel-in-progress: false
  group: release-plz-${{ github.ref }}
jobs:
  release:
    permissions:
      contents: write
      id-token: write
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
        persist-credentials: false
    - uses: dtolnay/rust-toolchain@stable
    - env:
        GITHUB_TOKEN: ${{ secrets.PAT }}
      uses: release-plz/action@v0.5
      with:
        command: release
  release-pr:
    permissions:
      contents: write
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
      with:
        fetch-depth: 0
        persist-credentials: false
    - uses: dtolnay/rust-toolchain@stable
    - env:
        GITHUB_TOKEN: ${{ secrets.PAT }}
      uses: release-plz/action@v0.5
      with:
        command: release-pr
name: Version
on:
  push:
    branches:
    - master
