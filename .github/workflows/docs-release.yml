# Auto-generated by gaji
# Do not edit manually - Edit workflows/docs-release.ts instead
# Generated at: 2026-02-23T22:50:19Z

jobs:
  update-versions:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
      with:
        ref: master
    - uses: actions/setup-node@v4
      with:
        node-version: '22'
    - name: Update versions.json
      run: node docs/scripts/update-versions.mjs ${{ github.ref_name }}
    - name: Commit and push
      run: |-
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add docs/versions.json
        git commit -m "docs: update versions.json for ${{ github.ref_name }}"
        git push
name: Docs Release
on:
  push:
    tags:
    - v*
