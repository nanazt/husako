# Auto-generated by gaji
# Do not edit manually - Edit workflows/sync-workflows.ts instead
# Generated at: 2026-02-23T17:41:24Z

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
      with:
        token: ${{ secrets.PAT }}
    - name: Install gaji
      run: npm install -g gaji
    - name: Generate types
      run: gaji dev
    - name: Build workflows
      run: gaji build
    - name: Commit and push if changed
      run: |-
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add .github/workflows/
        git diff --staged --quiet || (git commit -m "chore: sync workflow YAML" && git push)
name: Sync Workflows
on:
  push:
    branches:
    - master
    paths:
    - workflows/**
